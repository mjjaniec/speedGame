<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="pl.edu.agh.io.android.activities"
          android:versionCode="1"
          android:versionName="1.0">

    <uses-sdk android:minSdkVersion="10"/>

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:label="@string/common__app_name"
        android:icon="@drawable/clock"
        android:theme="@android:style/Theme.Holo">

        <activity android:name=".EmptyActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity android:name=".RanksActivity"/>
        <activity android:name=".LoginActivity"/>
        <activity android:name=".OfflineActivity" android:label="@string/offline__title"/>
        <activity android:name=".RegisterActivity" android:label="@string/register__title"/>
        <activity android:name=".ChooseFileActivity"/>
        <activity android:name=".NewGameActivity" android:label="@string/newgame__title"/>
        <activity android:name=".UsersActivity" android:label="@string/users__title"/>
        <activity android:name=".GameActivity" android:label="@string/game__title"/>

    </application>
</manifest>
